[project]
name = "in_my_area_api"
version = "0.1.0"

[tool.black]
line-length = 88
target-version = ['py310']


[tool.isort]
profile = "black"

[tool.pytest.ini_options]
addopts = ""


[tool.coverage.run]
omit = ["tests/*"]
